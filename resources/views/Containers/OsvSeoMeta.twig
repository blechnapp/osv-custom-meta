{# ========== OSV CUSTOM META (SSR) v1.4.0 DEBUG ========== #}
<!-- OSV-DEBUG: Template wird gerendert -->

{% set PROP_TITLE_ID = 288 %}
{% set PROP_DESC_ID  = 289 %}

<!-- OSV-DEBUG: isItem={{ services.template.isCurrentTemplate('tpl.item') ? 'JA' : 'NEIN' }} -->
<!-- OSV-DEBUG: itemDefined={{ item is defined ? 'JA' : 'NEIN' }} -->

{% if item is defined %}
  <!-- OSV-DEBUG: docsCount={{ item.documents|length }} -->
  {% if item.documents|length > 0 %}
    {% set d = item.documents[0].data %}
    <!-- OSV-DEBUG: dataKeys={{ d|keys|join(', ') }} -->
    <!-- OSV-DEBUG: hasProperties={{ d.properties is defined ? 'JA' : 'NEIN' }} -->
    <!-- OSV-DEBUG: hasVariationProperties={{ d.variationProperties is defined ? 'JA' : 'NEIN' }} -->
    {% if d.properties is defined %}
      <!-- OSV-DEBUG: propertiesCount={{ d.properties|length }} -->
      {% for p in d.properties %}
        <!-- OSV-DEBUG: prop id={{ p.property.id ?? p.id ?? 'UNKNOWN' }} -->
      {% endfor %}
    {% endif %}
    {% if d.variationProperties is defined %}
      <!-- OSV-DEBUG: variationPropertiesCount={{ d.variationProperties|length }} -->
      {% for p in d.variationProperties %}
        <!-- OSV-DEBUG: varProp id={{ p.property.id ?? p.id ?? 'UNKNOWN' }} -->
      {% endfor %}
    {% endif %}
  {% endif %}
{% endif %}

<!-- OSV-DEBUG-END -->
