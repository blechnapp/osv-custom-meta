{# ========== OSV CUSTOM META (SSR) v1.4.2 DEBUG ========== #}
<!-- OSV-DEBUG: Template wird gerendert -->

{% if item is defined and item.documents|length > 0 %}
  {% set d = item.documents[0].data %}

  <!-- OSV-DEBUG: item-keys={{ d.item|keys|join(', ') }} -->

  {% if d.item.properties is defined %}
    <!-- OSV-DEBUG: item.propertiesCount={{ d.item.properties|length }} -->
    {% for p in d.item.properties %}
      <!-- OSV-DEBUG: item.prop={{ p|json_encode }} -->
    {% endfor %}
  {% else %}
    <!-- OSV-DEBUG: item.properties=NICHT VORHANDEN -->
  {% endif %}

  <!-- OSV-DEBUG: variationProperties IDs: -->
  {% for p in d.variationProperties|default([]) %}
    <!-- OSV-DEBUG: varProp full={{ p|json_encode }} -->
  {% endfor %}

  <!-- OSV-DEBUG: properties IDs: -->
  {% for p in d.properties|default([]) %}
    <!-- OSV-DEBUG: prop full={{ p|json_encode }} -->
  {% endfor %}

  {% if d.characteristics is defined %}
    <!-- OSV-DEBUG: characteristicsCount={{ d.characteristics|length }} -->
    {% for c in d.characteristics %}
      <!-- OSV-DEBUG: characteristic={{ c|json_encode }} -->
    {% endfor %}
  {% else %}
    <!-- OSV-DEBUG: characteristics=NICHT VORHANDEN -->
  {% endif %}

{% else %}
  <!-- OSV-DEBUG: kein Item -->
{% endif %}

<!-- OSV-DEBUG-END -->
