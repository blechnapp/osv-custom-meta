{# OSVCustomMeta: SingleItemWrapper-Override #}
{% embed getTemplate('Ceres::Item.SingleItemWrapper') %}

  {% block PartialHead %}
    <meta name="x-osvcustommeta" content="active-wrapper"/>
    {# … hier deine Ausgabe/Logik … #}

    {# Beispiel: Meta-Partial wiederverwenden und nur Blöcke überschreiben #}
    {% embed getPartial('page-metadata') %}
      {% block title %}{{ (itemData.variation.model | default(itemData.texts.name1)) | striptags | trim }}{% endblock %}
      {% block description %}{{ (itemData.texts.metaDescription | default(itemData.texts.shortDescription | default(itemData.texts.description | striptags))) | striptags | trim }}{% endblock %}
    {% endembed %}
  {% endblock %}

{% endembed %}
