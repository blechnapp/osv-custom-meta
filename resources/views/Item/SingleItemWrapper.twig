{# OSVCustomMeta: Minimal-Test mit sichtbarem Marker #}
{% embed getTemplate('Ceres::Item.SingleItemWrapper') %}

{% block PartialHead %}
    {# Sichtbarer Debug-Marker (wird später entfernt) #}
    <div id="osv-debug-marker" style="position:fixed;top:0;left:0;background:yellow;z-index:99999;padding:5px;">
        OSV Template Override AKTIV (Version 1.0.22)<br>
        Timestamp: {{ "now"|date("Y-m-d H:i:s") }}
    </div>

    {# Standard Head-Block erstmal unverändert weitergeben #}
    {{ parent() }}
{% endblock %}

{% endembed %}
